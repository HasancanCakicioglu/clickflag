version: '3.8'

services:
  clickflag-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${DOCKER_CONTAINER_NAME:-clickflag-backend}
    ports:
      - "${DOCKER_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8080}
      - DATABASE_PATH=${DATABASE_PATH:-/app/data/countries.db}
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - LOG_LEVEL=${LOG_LEVEL:-info}
    volumes:
      # Database persistence
      - ./data:/app/data
      # Log persistence
      - ./logs:/app/logs
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - clickflag-network

networks:
  clickflag-network:
    driver: bridge
